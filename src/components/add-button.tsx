// "use client";
import { Button } from "@/components/ui/button";
import { PlusIcon } from "lucide-react";

const AddButton = () => {
  return (
    <Button size="icon">
      <PlusIcon />
    </Button>
  );
};

export default AddButton;
